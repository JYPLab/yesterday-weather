{
  "name": "yesterday-weather",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w server && ait build",
    "test": "npm run test -w shared"
  },
  "devDependencies": {
    "@vercel/node": "^5.6.20"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^2.4.3",
    "@toss/tds-mobile": "^2.3.0"
  }
}
